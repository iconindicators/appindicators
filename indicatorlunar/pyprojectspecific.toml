# THIS IS AN INCOMPLETE PYPROJECT.TOML
# USED BY THE BUILD WHEEL SCRIPT
# TO CREATE A COMPLETE PYPROJECT.TOML.
[project]
version = "1.0.101"
description = "Application indicator for the home astronomer."
classifiers = [
  "Topic :: Scientific/Engineering :: Astronomy" ]
#TODO If/when Skyfield is released, cannot do so for 32 bit,
# so release both Skyfield and PyEphem.
# For the dependencies below, remove ephem (and instead in the pip install,
# use bash trickery to determine if 32 bit or 64 bit and adjust the install
# line to include PyEphem or both PyEphem and Skyfield.
dependencies = [
  "ephem",
  "requests",
  "sgp4" ]
