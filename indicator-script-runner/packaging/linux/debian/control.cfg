%%DEPENDS%%=libnotify-bin, python3-notify2, pulseaudio-utils
%%DESCRIPTION_EXTENDED%%=The user can specify a terminal command or script with parameters and use the mouse (or keyboard shortcut where applicable) to execute the command/script rather than opening a terminal. Optionally the results can be displayed in the icon label.
